[project]
name = "nxuskit-codex-python-app-starter"
version = "0.9.4"
description = "Small Python starter for nxusKit Codex task recipes."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

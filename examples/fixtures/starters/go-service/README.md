# Go Service Starter

Use with the multi-provider, retry/fallback, solver, or vision recipes.

Example prompts:

```text
Use nxusKit to make this Go service support OpenAI and Ollama.
```

```text
Use nxusKit Examples to find the smallest retry/fallback example and implement that pattern here.
```

Expected first checks:

```bash
go test ./...
go run . -prompt "hello"
```

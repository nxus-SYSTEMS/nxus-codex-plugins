# Local Provider Python Starter

Use with the local Ollama recipe:

```text
Use local Ollama through nxusKit for this app.
```

Suggested environment:

```bash
export LOCAL_PROVIDER=ollama
export OLLAMA_BASE_URL=http://localhost:11434
export LOCAL_MODEL=llama3.2
```

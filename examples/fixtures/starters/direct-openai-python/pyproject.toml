[project]
name = "nxuskit-codex-direct-openai-starter"
version = "0.9.4"
description = "Direct OpenAI starter for nxusKit migration practice."
requires-python = ">=3.10"
dependencies = ["openai>=1.0.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

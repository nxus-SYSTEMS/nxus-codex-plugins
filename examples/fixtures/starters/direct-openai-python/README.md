# Direct OpenAI Python Starter

Use with the migration recipe:

```text
Migrate this OpenAI-only integration to nxusKit without changing app behavior.
```

The starter intentionally keeps the direct provider SDK call simple so Codex can preserve behavior while migrating it.

Credentials must come from environment variables. Do not paste API keys into chat.
